section.search
  .container
    .row
      .col-md-12
        form.relative.flex.align-center(action="")
          input.search--input(type="search", placeholder='Buscar')
          button.search--button.absolute(type='submit'): +svg('search')
